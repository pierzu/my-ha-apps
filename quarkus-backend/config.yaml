name: "Quarkus Backend"
version: "0.0.4"
slug: "quarkus-backend"
description: "Quarkus backend do testów lokalnych i uruchomienia w Home Assistant"
arch:
  - aarch64

startup: application
boot: auto

ports:
  8080/tcp: 8080

webui: "http://[HOST]:[PORT:8080]/ha/health"

image: "ghcr.io/pierzu/my-quarkus-backend"

homeassistant_api: true
hassio_role: homeassistant
